import { SegmentedControl } from './segmented-control';

describe('SegmentedControlComponent', () => {
  it('is pending until component is implemented', () => {
    expect(SegmentedControl).toBeDefined();
  });
});
